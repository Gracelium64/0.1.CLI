import 'functions/functions_grace64.dart';
import 'functions/main_functions.dart';
import 'functions/main_menu.dart';


void main() {

  clearConsole();
  splashScreen();
  clearConsole();
  mainMenu();
// create mainMenuHE and mainMenuDE, change printouts and cases and functions names along with translation
// if cases when mainMenu() loop ends for if mainMenu() gives a return of change language than another starts {else if}
  lineBreak();
  
}